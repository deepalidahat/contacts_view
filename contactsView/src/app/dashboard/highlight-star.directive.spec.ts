import { HighlightStarDirective } from './highlight-star.directive';

describe('HighlightStarDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightStarDirective();
    expect(directive).toBeTruthy();
  });
});
